# PickMe Go Backend

This folder is reserved for the PickMe Go backend.

Expected layout:

```text
backend/
├── cmd/
├── internal/
├── docs/
├── sql/
└── scripts/
```

Run backend:

```bash
go run ./cmd/server
```

Run backend tests:

```bash
go test ./...
```
