// Environment variable polyfill — intentionally a no-op.
// Supabase URL and publishable key MUST come from build-time import.meta.env
// (provided by Lovable Cloud). Hardcoded fallbacks were removed for security.
// Kept as a side-effect entry to preserve existing imports.
export {};
